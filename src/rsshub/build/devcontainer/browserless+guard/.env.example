# === BASE CONFIGURATION ===
# Project name for Docker Compose
COMPOSE_PROJECT_NAME=rsshub

NODE_ENV=production
CACHE_TYPE=memory
# Browserless
PUPPETEER_WS_ENDPOINT=ws://rsshub-browserless:3000


# Guard
ACCESS_KEY=rsshub

